/* function renderFullscreenPicture (data) {
  const picturesContainer = document.querySelector('.pictures  container');
  const imgElements = picturesContainer.querySelectorAll('.picture__img');

  function imgElementClickHandler (event) {
    //event.preventDefault();
    console.log(event.target)
  }
  console.log(data);

  imgElements.forEach((imgElement) => {
    imgElement.addEventListener('click', imgElementClickHandler);
  });
}

export { renderFullscreenPicture }; */
